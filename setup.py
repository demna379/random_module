from setuptools import setup, find_packages

setup(
    name="virtxa_module",       # pip install name
    version="0.1.0",
    packages=find_packages(),   # this finds virtxa_module automatically
    author="Guram",
    description="Homework Python module",
)
